export default function (app) {
    app.get('/users', async (req, res) => {
        return res.json({});

        // put this here as a reminder to do it later, doesn't do anything rn as u can see. -isaac
    });
}